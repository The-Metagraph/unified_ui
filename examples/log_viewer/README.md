# Unified Examples Log Viewer

This standalone Mix project demonstrates the `log_viewer` widget through the
shared example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary event
stream.
