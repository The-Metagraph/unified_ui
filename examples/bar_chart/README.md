# Unified Examples Bar Chart

This standalone Mix project demonstrates the `bar_chart` widget through the
shared example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
categorical chart.
