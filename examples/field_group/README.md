# Unified Examples Field Group

This standalone Mix project demonstrates the `field_group` construct through the
shared example-suite DSL template, theme, and style profile.

It keeps a shared form-oriented shell intact while foregrounding one primary field
grouping construct.
