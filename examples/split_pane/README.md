# Unified Examples Split Pane

This standalone Mix project demonstrates the `split_pane` widget through the shared
example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
dual-region layout.
