# Unified Examples Stream Widget

This standalone Mix project demonstrates the `stream_widget` widget through the shared
example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
append-only operational feed.
