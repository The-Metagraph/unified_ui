# Unified Examples Process Monitor

This standalone Mix project demonstrates the `process_monitor` widget through the shared
example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
process inventory.
