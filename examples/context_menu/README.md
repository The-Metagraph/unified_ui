# Unified Examples Context Menu

This standalone Mix project demonstrates the `context_menu` widget through the shared
example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
anchored action menu.
