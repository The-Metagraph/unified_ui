# Unified Examples Command Palette

This standalone Mix project demonstrates the `command_palette` widget through
the shared example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
quick-action surface.
