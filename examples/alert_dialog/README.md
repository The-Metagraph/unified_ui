# Unified Examples Alert Dialog

This standalone Mix project demonstrates the `alert_dialog` widget through the shared
example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
destructive confirmation surface.
