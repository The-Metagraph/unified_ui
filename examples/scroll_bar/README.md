# Unified Examples Scroll Bar

This standalone Mix project demonstrates the `scroll_bar` widget through the shared
example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary
viewport control.
