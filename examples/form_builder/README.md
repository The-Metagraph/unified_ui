# Unified Examples Form Builder

This standalone Mix project demonstrates the `form_builder` construct through the
shared example-suite DSL template, theme, and style profile.

It keeps a shared form-oriented shell intact while foregrounding one primary form
composition workflow.
