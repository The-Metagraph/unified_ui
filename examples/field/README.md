# Unified Examples Field

This standalone Mix project demonstrates the `field` construct through the shared
example-suite DSL template, theme, and style profile.

It keeps a shared form-oriented shell intact while foregrounding one primary field
composition construct.
