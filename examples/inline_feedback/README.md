# Unified Examples Inline Feedback

This standalone Mix project demonstrates the `inline_feedback` widget through
the shared example-suite DSL template, theme, and style profile.

It keeps the shared example shell intact while foregrounding one primary inline
message surface.
