# Elm Assets

This directory is reserved for the `web_ui` frontend runtime.

The scaffold phase keeps the asset boundary explicit without committing to a
full Elm application yet.

Planned layout:

- `src/` for frontend runtime entry modules and widget rendering
- `tests/` for frontend runtime tests and fixtures
