# Maintainer Workflows

Recommended package-local checks:

- `mix deps.get`
- `mix test`
- `mix docs`

Use `WebUi.Reference.package_reference/0` and `WebUi.Info.package_summary/0`
while expanding the package scaffold.
