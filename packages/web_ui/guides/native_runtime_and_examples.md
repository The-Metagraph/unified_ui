# Native Runtime And Examples

Direct native usage starts with `WebUi.Widgets` and `WebUi.Examples`.

Use `WebUi.Widgets.screen/4` to build a native screen and mount it through
`WebUi.Runtime.mount_native_screen/2`.
