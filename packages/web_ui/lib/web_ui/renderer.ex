defmodule WebUi.Renderer do
  @moduledoc """
  Canonical IUR rendering through the Phoenix + Elm pipeline.

  This area provides the rendering logic that transforms canonical
  UnifiedIUR representations into:
  - Phoenix template structures for server-side rendering
  - Elm view definitions for client-side rendering
  - Bridge data for hydration and interop

  The renderer ensures that canonical IUR from the unified_ui DSL
  is correctly rendered in both server and client contexts.
  """
end
